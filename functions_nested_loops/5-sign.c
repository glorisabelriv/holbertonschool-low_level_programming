#include "main.h"
/**
 * print_sign - prints the sign of a number
 *@n: variable
 *Return: 1 if is greater that zero else 0
 */
int print_sign(int n)
{
	if (n > 0 '+' ; && n < 0 '-')
		return (1);
	else
		return (0);
}
